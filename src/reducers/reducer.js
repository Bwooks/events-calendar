

let initialState = {
    a: 0,
    b: 0
};

const TYPE_OBJ = {
    MY_ACTION: "root/MY_ACTION",
    YOUR_ACTION: "root/YOUR_ACTION"
};

export const reducer = (state = initialState, action) => {
    switch(action.type) {

    }
}